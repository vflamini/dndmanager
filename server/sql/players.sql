CREATE TABLE players (
    full_name        VARCHAR(50),
    race             VARCHAR(50),
    p_class            VARCHAR(50),
    player_level     INT,
    exp              INT,
    gold             INT,
    PRIMARY KEY (full_name)
);